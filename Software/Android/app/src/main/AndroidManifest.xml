<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.fhnw.pro4e_team1_sphere">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_sphere"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_sphere"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowFullscreen="true">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".SubMenu"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <!--
        <activity
            android:name=".QuizScreen"
            android:parentActivityName=".SubMenu"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>
        <activity
            android:name=".CardScreen"
            android:parentActivityName=".SubMenu"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>
        <activity
            android:name=".TypeScreen"
            android:parentActivityName=".SubMenu"
            android:screenOrientation="portrait"
            android:configChanges="keyboard"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>
        -->
    </application>

</manifest>